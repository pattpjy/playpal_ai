interface User {
  userName: string;
  userId: string;
  childName: string;
  childAge: number;
  childInterest: string;
}

export const User = (user: User) => {
  const user_info = user;
  return user_info;
};
